[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmd = "npm ci --omit=dev --prefer-offline"

[phases.build]
cmd = "npm run build"

[start]
cmd = "npm start"

[variables]
NPM_CONFIG_CACHE = "/tmp"
